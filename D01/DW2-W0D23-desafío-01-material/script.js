        // Generar un número aleatorio entre 1 y 100
        const numeroCorrecto = Math.floor(Math.random() * 100) + 1;

        // Número de intentos disponibles
        const intentosMaximos = 5;
        let intentosRealizados = 0;

        // Abrir el bucle (pista: aquí usaremos la cantidad de intentos maximos y la de intentos realizados)

        // Pedir al jugador que ingrese un número

        // Validar si el jugador ingresó un número válido

        // Si es válido, incrementar el número de intentos realizados

        // Comparar el número elegido con el número correcto
        
        // Si el jugador ha adivinado, se informa en consola y se sale del bucle while
        
        // Si el jugador no ha adivinado, proporcionar una pista en consola sobre si el número es mayor o menor

        // Si se alcanza el máximo de intentos sin adivinar, mostrar el número correcto